# Optional login gate. Set both to require a username/password before the
# app can be used at all; leave either unset (the default) to keep the app
# open with no login, same as before this feature existed.
CLINEHUB_USER=
CLINEHUB_PASSWORD=
